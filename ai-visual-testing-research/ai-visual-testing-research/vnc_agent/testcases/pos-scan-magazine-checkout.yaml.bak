id: pos-scan-magazine-checkout-001
name: 任务视图切换ScannerSimulator扫描杂志并现金结账
mode: explicit
target_id: win10-test-01
timeout_seconds: 420

# Start from empty cart on POS sales screen when visible; if Task View already open, later steps recover.
precondition:
  facts:
    - key: environment_ready
      spec:
        operator: all
        conditions:
          - type: visual_question
            value: >-
              当前画面是否大致可用：要么是 Windows 任务视图（多窗口缩略图深蓝背景），
              要么是 POS 主画面（可见合計/小計等）？
              仅当不是完全黑屏/断开时回答 passed。

action_tags:
  - tag: open_task_view
    matcher:
      action_type: hotkey
  # Do not match title text "Scanner*" — grounder was latching title bar at y≈50.
  - tag: select_scanner
    matcher:
      action_type: click
  - tag: focus_barcode
    matcher:
      action_type: double_click
      target_text_contains: "Barcode"
  - tag: type_barcode
    matcher:
      action_type: type_text
  - tag: click_scan
    matcher:
      action_type: click
      target_text_contains: "Scan"
  - tag: focus_pos
    matcher:
      action_type: hotkey
  - tag: subtotal
    matcher:
      action_type: click
      target_text_contains: "小計"
  - tag: cash_start
    matcher:
      action_type: click
      target_text_contains: "預/現計"
  - tag: cash_finalize
    matcher:
      action_type: click
      target_text_contains: "確定"

steps:
  - id: open-task-view
    name: 打开Windows任务视图
    intent: >-
      本步骤唯一动作：使用 action_type=hotkey，keys 为 ["win", "tab"]（即 Win+Tab），
      打开 Windows 任务视图（Task View）。
      不要 click 任何窗口；不要只按 Win；不要 Escape。
      成功后应出现深蓝背景上的多个应用缩略图网格。
    max_retries: 2
    verification_mode: business
    expected:
      operator: all
      conditions:
        - type: visual_question
          value: >-
            当前是否为 Windows 任务视图：深蓝/蓝色桌面背景上排列多个窗口缩略图，
            标题可能带省略号（如 ScannerSi...、CT6100ModeSelfSimulatorF...）？
            仅当明确是任务视图多窗口网格时回答 passed。
            若仍是单个全屏应用、或看不到缩略图网格，不要回答 passed。

  - id: select-scanner-simulator
    name: 在任务视图点击ScannerSimulator预览区
    intent: >-
      本步骤唯一动作：click 一次，把前台切到 ScannerSimulator。
      【target 字段硬性规则】
      - target.text 必须为 null（不要填 Scanner / ScannerSi / ScannerSimulator / 任何窗口标题）。
      - target.role 可为 null 或 "window_preview"。
      - target.description 只描述几何与预览内容，不要把标题字符串当作可点文字。
      - nearby_texts 必须包含可见标题 ScannerSimulator / ScannerSi... / ScannerS...，
        但该标题只作锚点，不是 click 目标。
      【相对标题定位（窗口位置和排列可能变化）】
      - 先找到以 Scanner 开头的标题文字；完整或带省略号都可以。
      - 从该标题文字的水平中心，垂直向正下方移动约 50–100 像素，
        点击同一缩略图内部的浅色预览区域。
      - 落点必须在标题正下方的多行黑色条码列表中，不能仍落在标题文字或标题图标上。
      - 若标题下方预览很窄，仍以该标题水平中心向下进入预览区为准；
        不要根据它当前处于第几排、第几个或任何绝对 x/y 坐标判断。
      - Scanner 预览特征是非常密集的多行条码/文本列表；借此与 POS 数字键、
        CT6100 表单日志、CashChanger 表格、文件夹和红色 X 窗口区分。
      【禁止】
      - 禁止 click 任何标题文字条；禁止点深蓝空白、顶部栏、其它模拟器、POS 小键盘缩略图。
      - 禁止 Win+Tab、Escape。
    max_retries: 4
    verification_mode: business
    expected:
      operator: all
      conditions:
        - type: visual_question
          value: >-
            是否已离开 Windows 任务视图，且前台明确是 ScannerSimulator：
            同时可见顶部「Barcode:」标签及其输入框、中部「Favorite:」条码列表、
            右下角「Scan」按钮？仅当这三处都明确可见时回答 passed。
            若窗口标题为 CT6100ModeSelfSimulatorForm、画面是决済情報/応答方法/日志区域，
            或仍是深蓝底多窗口缩略图、POS、其它应用，必须回答 failed，不要 passed。

  # ScannerSimulator UI: label "Barcode:" + text box under it; Favorite grid;
  # bottom-right "Scan" button. Double-click the field to select its current value.
  - id: focus-barcode-field
    name: 双击Barcode输入框全选原条码
    intent: >-
      本步骤唯一动作：使用 action_type=double_click，双击 ScannerSimulator 窗口顶部「Barcode:」标签
      正下方的白色输入框内部（当前通常显示默认条码 2000900010268）。
      双击输入框中的现有数字文本，使完整条码呈蓝色高亮的全选状态。
      不要点 Favorite 表格行、Scan、TopMost 或窗口外的 POS 控件。
      禁止单击后结束本步骤；禁止 Win+Tab、Escape、Ctrl+A、Backspace。
    max_retries: 2
    verification_mode: business
    expected:
      operator: all
      conditions:
        - type: visual_question
          value: >-
            ScannerSimulator 是否仍在前台，且「Barcode:」下方输入框内的原条码
            是否像参考图一样完整呈蓝色高亮全选状态？
            仅当输入框中的整段现有条码已被选中时回答 passed。
            若只有插入光标、仅选中部分数字，或焦点在 Favorite 表格/Scan/其它控件，
            必须回答 failed。

  - id: type-barcode-45127366
    name: 输入条码45127366
    intent: >-
      本步骤唯一动作：action_type=type_text，text_value 为字符串 "45127366"
      （八位数字，不要空格或其它字符）。
      Barcode 输入框中的原条码应已完整高亮选中，直接输入以覆盖所选内容；
      不要先 Backspace；不要 Ctrl+A；
      不要 click Scan；不要点 Favorite 里的「雑誌(JAN)」行（本 case 用手动输入 45127366）。
      禁止 Escape、Win+Tab。
    max_retries: 2
    verification_mode: business
    expected:
      operator: all
      conditions:
        - type: visual_question
          value: >-
            「Barcode:」下方输入框中显示的内容是否明确为完整的 45127366
            （八位，不多不少）？
            仅当框内为 45127366 时回答 passed。
            若为空、缺位、多位或其它数字，不要回答 passed。

  - id: click-scan-button
    name: 点击Scan发送条码
    intent: >-
      本步骤唯一动作：click 一次 ScannerSimulator 窗口右下角的「Scan」按钮
      （白色按钮，文案 Scan，在 TopMost 复选框右侧）。
      不要点 TopMost 复选框；不要点 Favorite 表格；不要再改 Barcode 内容。
      禁止 Escape、Win+Tab。
    max_retries: 2
    verification_mode: business
    expected:
      operator: all
      conditions:
        - type: visual_question
          value: >-
            是否已点击 Scan 并完成一次扫描发送？
            可依据：按钮按下反馈、状态栏变化（如 DataCount/Idle 等）、
            或 Barcode 流程已执行完毕的界面迹象。
            仅当有合理依据认为 Scan 已执行时回答 passed。
            若 Scan 明显未点、仍停在未发送状态，不要回答 passed。

  - id: return-to-pos-via-task-view
    name: 再次打开任务视图以回到POS
    intent: >-
      本步骤唯一动作：hotkey ["win", "tab"] 再次打开任务视图。
      不要 click；不要 Escape。
    max_retries: 2
    verification_mode: business
    expected:
      operator: all
      conditions:
        - type: visual_question
          value: >-
            是否再次出现 Windows 任务视图的多窗口缩略图网格？
            仅当任务视图可见时回答 passed。

  - id: select-pos-main
    name: 在任务视图切回POS主画面
    intent: >-
      本步骤唯一动作：click 一次 POS 销售主画面缩略图的预览区中心，回到 POS。
      【target 规则】
      - target.text 必须为 null（不要用窗口标题当 target.text）。
      - 点预览图内部（数字小键盘/クリア/レジ袋/合計 可见的那块中心），不要点标题条。
      - 常见位置：任务视图偏左上或中上的大缩略图；点击 y 应落在预览内容区而非 y≈50 标题。
      【禁止】Scanner 列表预览、CashChanger、各 Simulator、资源管理器、TRAN4U、深蓝空白。
      禁止 Win+Tab、Escape。
    max_retries: 4
    verification_mode: business
    expected:
      operator: all
      conditions:
        - type: visual_question
          value: >-
            是否已离开任务视图，且前台为 POS 销售主画面
            （可见合計/商品区/小計等），不是 ScannerSimulator、不是多缩略图网格？
            仅当 POS 主画面为前台时回答 passed。

  - id: verify-scanned-item-in-cart
    name: 确认扫描商品已加入购物车
    intent: >-
      本步骤不要再扫描。刚通过 ScannerSimulator 发送了条码 45127366
      （Favorite 中对应 JAN8 一行；与「雑誌(JAN)」4910026530008 不同）。
      若商品尚未出现在左侧明细，可 wait 等待 POS 刷新；已入车则 wait。
      禁止点小計/支付/Escape。
    max_retries: 3
    verification_mode: business
    expected:
      operator: all
      conditions:
        - type: text_appears
          value: "点数"
        - type: visual_question
          value: >-
            POS 左侧商品明细或合计区域是否已因扫描条码 45127366 而登记至少 1 点商品
            （名称可能为 JAN8 或主档商品名，不一定显示「雑誌」），
            且合計绿色数量 ≥ 1 個、合计金额大于 0？
            仅当扫描商品明确在购物车中时回答 passed。
            若合計仍为 0 個、明细为空或无法确认，不要回答 passed。

  - id: calc-subtotal
    name: 计算合计金额
    intent: >-
      点击 POS 主画面的「小計」按钮，进入支付状态。
      不要点預/現計、金券、取引中止；禁止 Escape。
    max_retries: 2
    verification_mode: business
    expected:
      operator: all
      conditions:
        - type: text_appears
          value: "不足"
        - type: visual_question
          value: >-
            POS 是否已进入支付状态：左侧显示不足額（或お預かり金額），
            右侧可见金券/預/現計等支付按钮，且合计金额大于 0？
            仅当已进入支付画面时回答 passed。

  - id: start-cash-payment
    name: 启动现金支付并等待自动入金
    intent: >-
      点击 POS 主画面的「預/現計」按钮启动现金支付。
      找零机模拟器会自动投入现金，不要点击或操作模拟器窗口；
      等待 POS 现金入金对话框显示自动入金结果（預り金/お釣り/確定）。
      禁止 Escape。
    max_retries: 2
    verification_mode: business
    expected:
      operator: all
      conditions:
        # Do not hardcode お釣り: magazine price may vary by master data.
        - type: text_appears
          value: "10,000"
        - type: visual_question
          value: >-
            POS 主画面的现金入金对话框是否已显示自动入金结果：
            預り金明确为 10,000（或等价 10000），お釣り为非负且等于 預り金减去本次应付，
            并且对话框中的「確定」按钮可点击？
            仅当入金对话框三项都明确成立时回答 passed；不要检查找零机模拟器窗口。

  - id: finalize-cash-payment
    name: 确认完成现金结账
    intent: >-
      点击 POS 现金入金对话框中的「確定」按钮完成结账；
      不要点閉じる，不要操作找零机模拟器；禁止 Escape。
    max_retries: 2
    verification_mode: business
    expected:
      operator: all
      conditions:
        - type: visual_question
          value: >-
            POS 主画面是否已出现醒目的大号红色「済」完成标记，表示本次交易已固定完成？
            仅当「済」明确可见时回答 passed；
            若仍显示入金对话框、未完成或无法确认，不要回答 passed。
