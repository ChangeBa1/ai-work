# Invalid: missing required fields (name, target_id, mode)
id: bad-001
steps:
  - id: s1
    name: step
    intent: do something
    expected:
      operator: all
      conditions:
        - type: text_appears
          value: "x"
