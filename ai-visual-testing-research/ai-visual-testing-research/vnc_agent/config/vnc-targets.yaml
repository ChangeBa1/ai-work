# VNC target definitions. Passwords are environment-variable references only (FR-047).
targets:
  - id: win10-test-01
    host: "192.168.8.122"
    port: 5900
    password_env: "VNC_AGENT_VNC_PASSWORD"
    connect_timeout_seconds: 15
    reconnect_attempts: 3
