# Core Loop / Verification / Model-Interface Requirements Checklist: VNC 黑盒 GUI 自动化测试核心执行闭环

**Purpose**: 实现前的正式需求质量门禁（Formal pre-implementation gate）——检验 spec.md/
plan.md/data-model.md/contracts/ 中"核心闭环与状态机"、"验证独立性与失败处理"、"模型接口与
可替换性"三个领域的需求本身是否完整、清晰、一致、可衡量，覆盖足够的场景与边界情况。
**本清单不验证实现是否正确，只验证需求文档是否写得足够好，足以支撑正确实现。**
**Created**: 2026-07-21
**Feature**: [spec.md](../spec.md) ｜ [plan.md](../plan.md) ｜ [data-model.md](../data-model.md) ｜ [contracts/](../contracts/)

**Note**: This checklist is generated by the `/speckit-checklist` command based on feature
context and requirements.

**执行记录（2026-07-21 自动化需求质量门禁）**：34 项逐条核对，其中 20 项在核对时发现
缺失/歧义/不一致，直接修订了 spec.md/data-model.md/research.md/
contracts/model-provider-contract.md/quickstart.md/tasks.md 后转为 PASS；1 项（CHK012）
因涉及需产品侧决策的业务权衡，曾标记 BLOCKED，已由需求方确认方案 A 并记录为
Clarification 2026-07-21，随后转为 PASS。**最终 34/34 项 PASS，0 项遗留 FAIL/BLOCKED。**
完整证据与修订细节见对应对话记录的门禁报告。

## 核心闭环与状态机 · Requirement Completeness

- [x] CHK001 每个 `AgentState` 取值（含 `RECOVERING`、`CANCELLED`）的进入/退出条件是否都被
      显式枚举，而不是只描述了主路径（`OBSERVING→…→VERIFYING`）？ [Completeness,
      data-model.md §12]
- [x] CHK002 `TestStep.status` 的五个取值（`pending`/`running`/`passed`/`failed`/
      `cancelled`）是否都有对应的进入条件说明，而不是只定义了取值范围？ [Completeness,
      data-model.md §2]
- [x] CHK003 `TestRun.status` 从各 `StepRecord.final_status` 聚合得出的规则是否覆盖了
      "取消"这一分支（而不仅是 passed/failed 两种）？ [Completeness, data-model.md §12]

## 核心闭环与状态机 · Requirement Clarity

- [x] CHK004 区分 `STEP_COMPLETED_PASSED` 与 `STEP_COMPLETED_FAILED` 的判定条件，对"预算
      耗尽时的 uncertain"这一具体子情形是否表述得足够明确、不需要读者自行推断？ [Clarity,
      data-model.md §12]
- [x] CHK005 "该步骤之后的所有步骤不再执行"这一表述，是否明确覆盖了"当前失败步骤之前已经
      passed 的步骤是否需要回滚/保留其状态"这一问题？ [Clarity, data-model.md §12]

## 核心闭环与状态机 · Requirement Consistency

- [x] CHK006 FR-035 的文字表述（"验证未通过时 MUST NOT 继续执行后续步骤"）与 data-model.md
      §12 的状态转移表述、以及 tasks.md T081b 的实现描述三处，对"步骤失败后的处理方式"的
      措辞是否完全一致，不存在可被不同实现者读出不同行为的空间？ [Consistency, Spec §FR-035,
      data-model.md §12]
- [x] CHK007 spec.md Key Entities 中"测试步骤"词条对"多轮 ActionIteration"的描述，是否与
      data-model.md §2/§9/§12 中关于 `max_retries` 共享预算的表述保持一致（同一份"预算"
      语义有没有在某处被不小心描述成独立计数器）？ [Consistency, Spec Key Entities,
      data-model.md §2]

## 核心闭环与状态机 · Acceptance Criteria Quality

- [x] CHK008 SC-001"完成一次完整的观察→…→保存证据闭环，且每个阶段均有可查阅的记录"是否
      给出了客观可核对的"完整"判定标准（例如：每个阶段都必须产生哪些可查询字段），而不是
      停留在定性描述？ [Measurability, Spec §SC-001]
- [x] CHK009 SC-002"此情况的发生比例为零"是否配套定义了具体的审查/抽样方法，使其在验收
      阶段可被客观核对，而不仅是一个定性目标？ [Measurability, Spec §SC-002]

## 核心闭环与状态机 · Scenario Coverage

- [x] CHK010 是否定义了"用例仅含一个 TestStep，且该步骤第一轮迭代即失败（此前没有任何已通过
      步骤）"这一场景下的需求（TestRun.status 聚合规则在零已通过步骤时是否仍然适用）？
      [Coverage, data-model.md §12]
- [x] CHK011 是否定义了"最后一个 TestStep 失败"与"中间某个 TestStep 失败"两种情形下，
      TestRun 终止逻辑是否需要区别对待（还是完全一致）？ [Coverage, Gap]

## 核心闭环与状态机 · Edge Case Coverage

- [x] CHK012 是否定义了"StepController 共享预算恰好在触发 VNC 断线恢复的同一轮迭代耗尽"
      这一边界情形下，`restart_step` 与"预算耗尽判定为 failed"两者的优先级？ [Edge Case,
      Gap, data-model.md §2/§8] — 已由需求方确认方案 A（预算优先生效，MUST NOT 尝试
      `restart_step`），记录为 Clarification 2026-07-21（spec.md Clarifications、FR-039、
      data-model.md §12、tasks.md T078）。
- [x] CHK013 是否定义了运行期间用户/系统取消请求恰好发生在某一轮 `ActionIteration` 执行
      中途时，该步骤 `iterations` 中"正在进行的这一轮"应记录为何种最终状态？ [Edge Case,
      Gap]

## 核心闭环与状态机 · Non-Functional Requirements

- [x] CHK014 `StepRecord.stage_durations_ms` 的计时粒度/精度要求是否被明确指定（毫秒级
      是否要求单调时钟、是否要求跨进程重启后仍可比较），还是仅给出字段名未给出精度要求？
      [Clarity, data-model.md §9]

## 核心闭环与状态机 · Dependencies & Assumptions

- [x] CHK015 "每个新 TestStep 开始时 `iteration_index` 重置为 0"这一假设，是否作为显式
      需求写出，而不是需要读者从状态转移图中自行推断？ [Assumption, data-model.md §12]

---

## 验证独立性与失败处理 · Requirement Completeness

- [x] CHK016 data-model.md §8 是否为 12 种 `FailureType` 中的每一种都明确给出了对应的
      `RecoveryAttempt.strategy`，还是只对部分类型（如 `target_not_found`、
      `grounding_low_confidence`、`vnc_disconnected`）给出了路由规则？ [Completeness,
      data-model.md §8]
- [x] CHK017 FR-037 列出的 9 种恢复策略中，是否每一种都在 data-model.md 或 research.md
      中说明了其适用的 `FailureType`，还是存在"策略已定义但未说明何时触发"的情况？
      [Completeness, Spec §FR-037]

## 验证独立性与失败处理 · Requirement Clarity

- [x] CHK018 `grounding_low_confidence` 所依赖的两个阈值（整体置信度阈值、Top1-Top2
      差值阈值）是否要求在实现前给出具体默认值，还是"由实现阶段依据实测调优"这一表述被
      当作可以无限期搁置的开放项？ [Clarity, research.md §11]
- [x] CHK019 FR-032"确定性方法无法判断时才使用视觉模型"中"无法判断"的边界是否被量化
      （例如：哪些验证器返回状态算作"无法判断"），还是留给实现者自行解释？ [Ambiguity,
      Spec §FR-032]

## 验证独立性与失败处理 · Requirement Consistency

- [x] CHK020 FR-033 的三态（passed/failed/uncertain）复合断言规则与 data-model.md §7
      给出的 `all`/`any` 聚合算法，在 spec.md、data-model.md、quickstart.md 三处对"不确定
      传染性"的用词是否完全一致？ [Consistency]
- [x] CHK021 spec.md Key Entities 中对"定位结果"的三类原因描述（未找到/整体偏低/
      Top1-Top2 接近）与 data-model.md §8 `GroundingLowConfidenceReason` 的取值定义是否
      一一对应，没有遗漏或多出的分类？ [Consistency, Spec Key Entities, data-model.md §8]

## 验证独立性与失败处理 · Acceptance Criteria Quality

- [x] CHK022 FR-038"MUST NOT 出现无限重试或无限循环"是否配套了可核对的验收方式（例如：
      审查每个 `strategy` 的 `max_retries` 配置是否均为有限正整数），而不仅是一句禁止性
      表述？ [Measurability, Spec §FR-038]

## 验证独立性与失败处理 · Scenario Coverage

- [x] CHK023 是否定义了"某个恢复策略自身执行超时或抛出异常"（即恢复动作本身失败）时的
      需求，还是只定义了"被恢复的原始失败"这一层？ [Coverage, Gap]
- [x] CHK024 是否定义了同一轮 `ActionIteration` 内连续触发两种不同 `FailureType`（例如
      先 `target_not_found` 恢复后又转为 `action_no_effect`）时，恢复记录与预算消耗应
      如何叠加计算的需求？ [Coverage, Gap]

## 验证独立性与失败处理 · Edge Case Coverage

- [x] CHK025 是否定义了单个 `FailureType` 的 `RecoveryAttempt.max_retries` 先于共享的
      `TestStep.max_retries` 耗尽时，两个上限谁优先生效的需求？ [Edge Case, data-model.md §8]

## 验证独立性与失败处理 · Non-Functional Requirements

- [x] CHK026 每类恢复策略的冷却时间（cooldown）是否被要求给出具体单位与取值范围，还是
      仅以"可配置"一词带过、没有默认值或边界约束？ [Clarity, Constitution 恢复与重试门禁]

## 验证独立性与失败处理 · Dependencies & Assumptions

- [x] CHK027 FR-032 的 `visual_question` 验证条件对新定义的 `describe_screen` 模型契约的
      依赖，是否在 spec.md 的 Key Entities 或 Functional Requirements 中有显式交叉引用，
      还是只存在于 data-model.md/contracts/ 而 spec.md 本身未提及？ [Traceability, Spec
      Key Entities]

---

## 模型接口与可替换性 · Requirement Completeness

- [x] CHK028 是否定义了新注册的 Planner/Grounder 实现在启动时应如何被校验（例如：契约
      一致性检查、必需方法是否齐全），还是只说明了"通过配置字段装配"而未说明装配失败时
      的处理需求？ [Gap, Spec §FR-046]

## 模型接口与可替换性 · Requirement Clarity

- [x] CHK029 `describe_screen` 对一个新的 Planner 供应商实现而言，是否被要求为 MUST
      实现的方法，还是允许某些精简/历史供应商只实现 `plan` 而不实现
      `describe_screen`？如果允许，FR-010/FR-032 对应调用点应如何降级的需求是否已给出？
      [Ambiguity, contracts/model-provider-contract.md]

## 模型接口与可替换性 · Requirement Consistency

- [x] CHK030 `plan`/`describe_screen`/`ground` 三个方法的调用超时要求，在 plan.md
      Performance Goals（Planner 60s、Grounder 30s）与
      contracts/model-provider-contract.md 之间是否一致——后者是否需要为
      `describe_screen` 单独声明超时，还是隐式复用 `plan` 的 60s？ [Consistency, plan.md
      Performance Goals]

## 模型接口与可替换性 · Acceptance Criteria Quality

- [x] CHK031 FR-046"Planner 组件 MUST 可替换"是否配套了客观的验收方式（例如：仅切换
      `models.yaml` 中的 `provider` 字段、不改动调用方代码即可完成替换并通过既有测试），
      还是只停留在定性的 MUST 表述？ [Measurability, Spec §FR-046]

## 模型接口与可替换性 · Scenario Coverage

- [x] CHK032 是否定义了"某 Planner 供应商实现声明支持 `plan` 但不支持 `describe_screen`"
      这一场景下系统应如何表现的需求（直接报错拒绝装配，还是允许装配但 FR-010/FR-032 相关
      功能自动降级）？ [Coverage, Gap]

## 模型接口与可替换性 · Ambiguities & Conflicts

- [x] CHK033 `describe_screen(mode="describe")` 在模型自身也无法给出确定描述时的响应
      要求是否已定义（例如返回低置信度 + 说明原因），还是契约只详细定义了
      `answer_question` 模式的"不确定"路径、"describe"模式的对应情形未覆盖？ [Gap,
      contracts/model-provider-contract.md]
- [x] CHK034 `describe_screen` 与 `plan` 必须共用同一 Planner 供应商配置这一约束，是否与
      "Planner 必须可替换"（FR-046）之间存在潜在张力（例如未来某团队想为纯视觉理解单独
      接入更便宜的模型）？该假设是否需要在 spec.md 中显式记录为已知限制？ [Assumption,
      contracts/model-provider-contract.md]

## Notes

- Check items off as completed: `[x]`
- 本清单测试的是需求文档本身的质量（完整/清晰/一致/可衡量/覆盖充分），不是实现是否正确；
  发现的问题应回到 spec.md/plan.md/data-model.md/contracts/ 修订，而不是直接改代码。
- 覆盖领域：核心闭环与状态机（CHK001-CHK015）、验证独立性与失败处理（CHK016-CHK027）、
  模型接口与可替换性（CHK028-CHK034）。安全与凭据要求（FR-047/048/049）本轮未纳入范围，
  如需要可另建 `security.md` 清单。
- CHK012 曾是本轮门禁唯一的 BLOCKED 项，已于 2026-07-21 由需求方确认方案 A 后解除阻塞。
- Items are numbered sequentially for easy reference.
